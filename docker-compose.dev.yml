# ─────────────────────────────────────────────────────────────
#  Dev override — maps local Mac storage directory into the container.
#  Usage:
#    docker compose -f docker-compose.app.yml -f docker-compose.dev.yml up -d --build
# ─────────────────────────────────────────────────────────────
services:
  app:
    volumes:
      - /Users/shadow/Freelance/E_MENU/resourse_document_backend/storage:/app/storage
    environment:
      SPRING_PROFILES_ACTIVE: dev
