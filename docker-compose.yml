# ─────────────────────────────────────────────────────────────
#  This file is kept for backwards compatibility.
#  Use the split files instead:
#
#    docker-compose.infra.yml  — Kafka infrastructure
#    docker-compose.app.yml    — Spring Boot application
#
#  Or use run.sh for easy control:
#    ./run.sh all start
# ─────────────────────────────────────────────────────────────
include:
  - docker-compose.infra.yml
  - docker-compose.app.yml
