# ─────────────────────────────────────────────────────────────
#  Dev profile — Docker Desktop
#  Windows host mount: /d/e_menu/resourse-srevice-backend
#  Start: docker compose -f docker-compose.app.yml up -d
# ─────────────────────────────────────────────────────────────
spring:
  datasource:
    url: jdbc:postgresql://host.docker.internal:5432/resource_service
    username: postgres
    password: "Hour1819"

  data:
    redis:
      host: host.docker.internal
      port: 6379

  kafka:
    bootstrap-servers: host.docker.internal:9092

resource:
  storage:
    base-path: /app/storage
    base-url: http://localhost:6060
