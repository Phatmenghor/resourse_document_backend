spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/notification_service
    username: postgres
    password: Hour1819

  kafka:
    bootstrap-servers: localhost:9092

resource:
  storage:
    base-path: ./storage
    base-url: http://localhost:5000
  kafka:
    topics:
      upload: resource-file-upload
      delete: resource-file-delete
    consumer:
      group-id: resource-file-group
