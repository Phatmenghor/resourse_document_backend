# Local profile — PostgreSQL running via docker-compose.infra.yml
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/notification_service
    username: postgres
    password: "Hour1819"

  data:
    redis:
      host: localhost
      port: 6379

  kafka:
    bootstrap-servers: localhost:9092

resource:
  storage:
    base-path: ./storage
    base-url: http://localhost:8080
